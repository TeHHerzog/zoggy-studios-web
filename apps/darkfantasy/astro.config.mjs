import { defineConfig } from "astro/config";

export default defineConfig({
  output: "static",
  outDir: "../../dist/darkfantasy",
  site: "https://darkfantasy.herzogit.com",
  base: "/darkfantasy"
});
